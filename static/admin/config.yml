backend:
  name: git-gateway
  branch: master


media_folder_relative: true
media_folder: src/pages/events
media_library:
  name: uploadcare
  config:
    publickey: b0adf2c4e2b5e2c100af
public_folder: /img

collections:
  - name: "Events"
    label: "events"
    identifier_field: title
    folder: "src/pages/events"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    preview: true
    fields:
      - label: "Title"
        name: "title"
        widget: "string"
      - label: "Publish Date"
        name: "date"
        widget: "datetime"
      - label: "Path"
        name: "path"
        widget: "string"
      - label: "Cover Image"
        name: "cover"
        widget: "image"
      - label: GalleryImage
        name: "gallery_image"
        widget: list
        field: {label: Image, name: image, widget: image}
      - label: "Body"
        name: "body"
        widget: "markdown"
  - name: "About"
    label: "about"
    identifier_field: title
    folder: "src/pages/about"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    editor:
     preview: true
    fields:
    - label: "Cover Image"
      name: "cover"
      widget: "image"
      media_library:
        config:
          multiple: true
    - label: Gallery Image
      name: "gallery_image"
      widget: image
      media_library:
        config:
          multiple: true
    - label: "Title"
      name: "title"
      widget: "string"
    - label: "Publish Date"
      name: "date"
      widget: "datetime"
    - label: "Path"
      name: "path"
      widget: "string"
    - label: "Body"
      name: "body"
      widget: "markdown"
